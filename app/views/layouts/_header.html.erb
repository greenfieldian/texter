<nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8" aria-label="Top">
    <% if user_signed_in = false %>
        <div class="w-full py-6 flex items-center justify-between border-b border-indigo-500 lg:border-none">
        <div class="flex items-center">
            <%= link_to root_path do %>
            <span class="sr-only">Workflow</span>
            <img class="h-10 w-auto" src="https://tailwindui.com/img/logos/workflow-mark.svg?color=blue" alt="">
            <% end %>
        </div>
        <div class="ml-10 space-x-4">
            <%= link_to new_user_session_path do %>
                <div class="inline-block bg-indigo-500 py-2 px-4 border border-transparent rounded-md text-base font-medium text-white hover:bg-opacity-75">Sign in</div>
            <% end %>

            <%= link_to new_user_registration_path do %>
                <div class="inline-block bg-white py-2 px-4 border border-transparent rounded-md text-base font-medium text-indigo-600 hover:bg-indigo-50">Sign up</div>
            <% end %>
        </div>
        </div>
    <% end %>

    <% if user_signed_in? %>
      <div class="w-full py-6 flex items-center justify-between border-b border-indigo-500 lg:border-none">
        <div class="flex items-center">
            <%= link_to root_path do %>
            <span class="sr-only">Workflow</span>
            <img class="h-10 w-auto" src="https://tailwindui.com/img/logos/workflow-mark.svg?color=blue" alt="">
            <% end %>

            <div class="hidden ml-10 space-x-8 lg:block">
                <%= link_to "People", people_path %>
                <%= link_to "Conversations", conversations_path %>
            </div>
        </div>
        <div class="ml-10 space-x-4">
            <%= link_to "Sign Out", destroy_user_session_path, :method => :delete %>
        </div>
      </div>
    <% end %>
</nav>